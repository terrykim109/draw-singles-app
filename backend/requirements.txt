flask==3.0.0
flask-cors==4.0.0

# sketch_matcher (the classifier). torch is a ~2 GB install with CUDA wheels;
# for CPU-only, install it first from the CPU index:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
torch>=2.0
torchvision>=0.15
pillow>=10.0
numpy>=1.24
